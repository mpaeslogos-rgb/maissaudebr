# @signpdf/placeholder-pdfkit010 declara peer-dependency pdfkit@~0.10.0, mas não
# importa o pacote pdfkit em runtime — só usa como referência de tipo. O projeto usa
# pdfkit@^0.19.1 (geração de atestados/receitas) sem conflito real de comportamento.
legacy-peer-deps=true
